# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "firewalld/files/defaults.yaml" as defaults %}

{# Merge in sshd:lookup pillar #}
{% set firewalld_settings = salt['pillar.get']('firewalld:lookup', default=defaults.firewalld, merge=True) %}
