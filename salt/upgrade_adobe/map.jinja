# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml (tplidr + "/defaults.yaml") as defaults %}

{# Merge in adobe:lookup pillar #}
{% set adobe_settings = salt['pillar.get']('adobe:lookup', default=defaults.adobe, merge=True) %}

